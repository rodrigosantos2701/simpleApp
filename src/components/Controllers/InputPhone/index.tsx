import React from 'react';
import { TextInputProps } from 'react-native';

import { Container } from './styles';

export function InputPhone({ ...rest }: any) {
  return <Container {...rest} />
}
